这是一个针对 PC、中低性能设备的语音输入法产品，核心目标是解决 PC 端语音输入在**准确率、唤起效率、专业词汇识别和使用体验**上的问题。

产品采用本地部署的轻量化 ASR 模型，尽量降低内存和计算资源占用，同时通过热词和可选 AI 模块，实现用户专业词汇和个人场景的个性化识别。

核心产品定位可以概括为：

**一个随时可以快速唤起、低延迟、本地运行，并且能够逐渐理解用户专业词汇的 PC 语音输入工具。**

# 一、产品价值

PC 端目前已经存在 macOS Dictation、Windows Voice Typing 等系统语音输入能力，因此产品的价值不是解决“PC 没有语音输入法”，而是进一步解决以下问题：

1. **快速输入**

用户不需要切换应用或打开单独的语音转写窗口，通过全局快捷键即可开始语音输入。

语音结束之后，识别结果直接输入到当前光标位置，使整个过程尽量接近键盘输入。

2. **本地运行**

ASR 默认在本地完成：

* 不依赖网络；
* 没有 API 调用成本；
* 延迟更加稳定；
* 语音内容不需要上传服务器；
* 更适合办公、学习和个人隐私场景。

3. **低性能要求**

模型需要能够在普通 PC 上流畅运行，同时尽可能减少 CPU、GPU 和内存占用，避免影响浏览器、IDE、Office 等其他应用。

4. **专业词汇个性化**

相比普通语音输入法，产品的重要差异化能力是通过 ASR 热词功能提高用户专属词汇的识别准确率。

例如：

* 人名；
* 公司名；
* 产品名；
* 技术术语；
* 课程名称；
* 项目名称；
* 用户经常使用的英文缩写。

用户可以主动配置，也可以通过后续 AI 模块自动生成和更新这些热词。

最终形成：

**Local ASR + Personalized Vocabulary**

即：

**越使用，越理解用户个人词汇的本地语音输入法。**

# 二、性能要求

目标设备以 PC 为主，包括 macOS 和 Windows。

主要性能目标：

1. 在当前主要测试设备上能够稳定、流畅运行；
2. ASR 运行期间尽量减少内存占用；
3. 不明显影响其他应用的正常使用；
4. Apple Silicon Mac 能够获得良好体验；
5. 必须在macbook neo（A18 pro）上可以轻松运行，最好能够支持 2019 年左右的 Intel Mac；
6. 用户停止讲话之后，应在较短时间内完成识别并输入文字；
7. 空闲状态下尽可能减少 CPU 和内存占用。

具体性能标准不在模型选型之前直接确定，而是在 Benchmark 阶段根据不同设备测试结果确定。

# 三、面向场景

产品定位为 PC 端语音输入法，使用方式类似手机语音输入。

主要适用于：

* 聊天；
* 搜索；
* 写邮件；
* ChatGPT 等 AI 对话；
* Word / Notion 等文档输入；
* IDE 中输入自然语言；
* 学习笔记；
* 专业场景中的术语输入。

核心要求包括：

* 性能要求低；
* 内存占用较少；
* 启动速度快；
* 转写延迟低；
* 中英文识别准确；
* 支持用户热词；
* 能够跨应用使用。

# 四、用户视角

## 1. 安装

用户下载并安装 APP。

首次打开完成基础初始化。

## 2. 初始化

用户可以设置：

* 全局唤起快捷键；
* 语音输入模式；
* 热词；
* 是否启用 AI 功能模块。

## 3. 输入交互

产品主要提供两种输入方式。

### Hold-to-talk

用户按住快捷键：

开始录音。

用户松开快捷键：

停止录音 → ASR 识别 → 文字直接输入当前光标位置。

整个交互为：

**按住 → 说话 → 松开 → 输入**

这是优先推荐的输入方式。

### Toggle 模式

用户按一次快捷键：

开始录音。

再次按快捷键：

停止录音 → ASR 识别 → 输入文字。

适合较长时间的语音输入。

## 4. 输入结果

识别结果优先：

**直接输入当前光标位置。**

例如用户正在：

* Chrome；
* ChatGPT；
* Word；
* Notion；
* VS Code；
* Slack。

将光标放在输入框中之后，通过快捷键说话，识别结果直接出现在当前位置。

同时保留其他方式：

* 保存识别结果；
* 点击图标复制；
* 自动进入剪贴板；
* 用户手动粘贴。

直接光标输入为主要方式，剪贴板作为补充和 fallback。

# 五、ASR 设计原则

ASR 是产品的核心基础能力。

模型主要需要满足：

1. 模型体积较小；
2. CPU / GPU 性能要求较低；
3. 内存占用较低；
4. 推理速度快；
5. 中文识别准确；
6. 英文识别准确；
7. 中英文混输能力较好；
8. 支持热词或者 Context Biasing；
9. 部署方便；
10. 能够在 macOS 和 Windows 上运行。

第一版不把 Streaming 作为硬性要求。

产品交互主要采用：

**录音 → 用户结束说话 → 完整识别 → 输入**

因此可以优先选择非流式模型。

这样能够降低：

* ASR 推理复杂度；
* Partial Result 管理；
* 稳定文本判断；
* UI 实时刷新；
* Chunk 管理；
* 流式状态管理。

只要用户结束讲话之后能够较快获得结果，就可以达到较好的输入法体验。

# 六、Benchmark

模型选型之前，首先建立统一 Benchmark。

Benchmark 的目的不是验证某一个模型，而是在统一数据和设备条件下比较不同 ASR 模型。

测试内容至少包括：

## 1. 普通中文

日常语言输入。

测试：

* 基础识别准确率；
* 标点；
* 长短句。

## 2. 普通英文

测试：

* 英文识别准确率；
* 常见表达；
* 长句。

## 3. 中英文混输

例如：

“我们可以把这个 model 放到 local environment 里面，然后测试 inference latency。”

重点测试 Code-switching 能力。

## 4. 专业术语

例如：

* Transformer；
* Paimon；
* Playwright；
* MCP；
* KV Cache；
* DSCI 554。

测试模型对于低频专有词的识别能力。

## 5. Hotword Benchmark

使用统一专业词测试集。

分别测试：

**不开启 Hotword**

和

**开启 Hotword**

比较专业词召回率。

核心指标：

**Hotword Recall**

例如测试集中存在 100 个专业词：

不开 Hotword：

62 / 100。

开启 Hotword：

91 / 100。

通过该指标直接评估不同模型的个性化能力。

## 6. 不同语速

包括：

* 慢速；
* 正常速度；
* 快速讲话。

## 7. 环境噪声

包括：

* 安静房间；
* 风扇；
* 键盘声音；
* 日常背景噪声。

## 8. 性能测试

在不同硬件上测试：

* 推理时间；
* Real Time Factor；
* CPU 使用率；
* GPU 使用率；
* 内存占用；
* 模型加载时间；
* 用户停止说话后的等待时间。

重点测试：

* Apple Silicon；
* Intel Mac；
* Windows 普通 PC。

Benchmark 完成之后，再决定最终 MVP ASR 模型。

# 七、AI 功能模块

AI 功能为可选模块，不影响基础语音输入能力。

用户可以完全关闭 AI 功能，仅使用本地 ASR。

AI 功能主要包括三个方向。

## 1. LLM 持续更新热词

用户可以上传自己的资料。

AI 对资料进行解析，识别：

* 专业术语；
* 人名；
* 项目名称；
* 公司名称；
* 产品名称；
* 用户经常使用的关键词。

自动生成热词。

用户也可以选择是否保存语音输入历史。

如果开启历史记录，AI 可以根据历史输入持续更新：

* 热词；
* 用户 Profile。

## 2. LLM 转写优化

ASR 首先生成原始文本。

AI 可以进一步对文本进行：

* 标点优化；
* 大小写修正；
* 明显错误修正；
* 表达整理。

用户可以选择是否开启。

## 3. 导出记录文档

如果用户开启历史记录，可以把语音输入记录：

* 保存；
* 搜索；
* 整理；
* 导出为文档。

# 八、实施路线

## 第一阶段：建立 Benchmark

首先准备测试数据和测试方法。

覆盖：

* 中文；
* 英文；
* 中英文混输；
* 专业术语；
* 热词；
* 不同语速；
* 不同噪音；
* 不同硬件。

建立统一评测指标。

## 第二阶段：ASR 模型选型

选择多个候选模型进行 PoC。

重点比较：

* Accuracy；
* Hotword Recall；
* RTF；
* 内存占用；
* CPU / GPU 使用；
* 模型大小；
* 部署复杂度。

基于 Benchmark 结果确定 MVP 模型。

## 第三阶段：MVP 开发

AI 功能暂时不开发。

MVP 主要包括：

* 全局快捷键；
* Hold-to-talk；
* Toggle 输入；
* 录音；
* 本地 ASR；
* 中英文输入；
* Hotword；
* 当前光标直接输入；
* 剪贴板 fallback；
* 输入状态图标；
* 基础 Settings。

MVP 的核心验证问题是：

**相比 macOS / Windows 自带语音输入，用户是否更愿意日常使用这个产品。**

## 第四阶段：完善 MVP

### Benchmark 持续优化

使用真实输入数据进一步评测：

* Accuracy；
* Hotword Recall；
* Latency；
* Memory；
* 用户实际错误 Case。

根据结果：

* 调整 ASR 参数；
* 调整 Hotword 参数；
* 优化模型；
* 必要时更换模型。

### 功能完善

优化：

* 输入交互；
* 快捷键；
* 状态提示；
* 权限管理；
* 生命周期管理；
* 模型加载；
* 后台资源占用；
* 异常处理；
* 跨应用兼容性。

## 第五阶段：开发 AI 功能

在基础输入体验稳定之后，再增加：

* 文档解析；
* 自动热词生成；
* User Profile；
* 历史记录；
* LLM 持续更新热词；
* LLM 文本优化；
* 记录导出。
