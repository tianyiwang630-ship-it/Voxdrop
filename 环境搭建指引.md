# 环境搭建指引

本项目使用 Python 3.11 和 uv。除 Homebrew 安装的系统工具外，Python、虚拟环境、缓存、模型和 NeMo-Speech.cpp runtime 都保存在项目目录内。

## 目录约定

```text
项目根目录/
├── .venv/                         # 公共 Python 环境
├── envs/
│   ├── sherpa/.venv/
│   ├── funasr/.venv/
│   ├── mlx/.venv/
│   └── faster-whisper/.venv/
├── runtimes/nemo-speech/          # NeMo-Speech.cpp
├── models/                        # 各模型文件
├── results/                       # 本地运行结果
└── .cache/                        # Python、uv 和模型仓库缓存
```

各 ASR 后端使用独立虚拟环境，避免 MLX、ONNX Runtime、FunASR 等依赖互相影响。所有 `pyproject.toml` 和 `uv.lock` 应保留；`.venv`、模型、runtime 和缓存不进入版本控制。

## 前置系统工具

macOS 上需要：

```bash
brew install ffmpeg cmake ninja sentencepiece
xcode-select -p
```

## 一键搭建

在项目根目录执行：

```bash
./scripts/setup-env.sh
```

脚本会：

1. 将 Python 3.11 安装到 `.cache/python`；
2. 同步公共环境和四个 ASR 后端环境；
3. 创建项目内模型、runtime 和结果目录；
4. 将 NeMo-Speech.cpp 安装到 `runtimes/nemo-speech`，不修改全局 `PATH`；
5. 执行完整环境验证。

重复执行是安全的：uv 会按锁文件同步，已存在的 NeMo-Speech.cpp 不会被覆盖。

## 日常使用

先加载项目内缓存和模型目录设置：

```bash
source scripts/project-env.sh
```

运行公共环境命令：

```bash
uv run --project . python --version
```

运行某个后端环境：

```bash
uv run --project envs/sherpa python your_script.py
uv run --project envs/funasr python your_script.py
uv run --project envs/mlx python your_script.py
uv run --project envs/faster-whisper python your_script.py
```

运行 NeMo-Speech.cpp：

```bash
./runtimes/nemo-speech/bin/nemo-speech --version
```

无需手动激活任何 `.venv`。

## 验证环境

```bash
./scripts/verify-env.sh
```

验证包括系统工具、Python 3.11、公共依赖、四个 Python ASR 后端和 NeMo-Speech.cpp。

## 修改依赖

公共环境：

```bash
source scripts/project-env.sh
uv add package-name
uv sync
```

指定后端：

```bash
source scripts/project-env.sh
uv add --project envs/sherpa package-name
uv sync --project envs/sherpa
```

修改依赖后应保留对应的 `pyproject.toml` 和 `uv.lock`。
