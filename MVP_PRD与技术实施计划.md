# 语音输入法第一版 MVP：PRD 与技术实施计划

> 状态：依据用户两轮问答整理，可作为后续 Codex 实施依据。
> 范围：Apple Silicon Mac、本地 Qwen ASR、个人日常使用。
> 本次交付：规划文档；应用开发、DMG 制作和发行不属于本次文档任务。
> 文中“用户确认”为已确定需求；“实施默认”为技术建议，可由后续实现根据验证结果调整，但不得改变已确认的用户行为。

## 1. 产品目标与范围

做一个常驻 macOS 的语音输入工具：用户把光标放在输入位置，通过快捷键录音，结束后获得 ASR 转写文本。结果写入系统剪贴板；目标输入位置仍可确认时，自动执行粘贴；所有有效转写保存在 App 内，用户可以删除。

首要场景是笔记和邮件，兼顾 Office 办公、浏览器、Mail、微信。无需切换系统输入法，也不要求目标应用安装插件。

### 1.1 用户已确认的决策

| 编号 | 决策 | 实施含义 |
| --- | --- | --- |
| D01 | 首版自己使用 | 优先日常可用，不设置外部用户规模或留存门槛 |
| D02 | 仅 Apple Silicon Mac | 首版不开发 Windows 和 Intel Mac runtime |
| D03 | 优先笔记/邮件 | 重点验证 Office、浏览器、Mail、微信中的光标粘贴 |
| D04 | Hold-to-talk、Toggle 都必须有 | 两种模式同时可用，分别设置快捷键，支持用户自定义 |
| D05 | 暂不分段，延迟标准用后再定 | 整段录音结束后识别，不提前开发流式、分段调度或实时拼接 |
| D06 | 只需 ASR 转写 | 不接 LLM，不承诺书面化、自动去口头语或理解口述修正 |
| D07 | 无论自动粘贴是否成功，结果都进入剪贴板 | 剪贴板是标准输出；不在粘贴后恢复旧剪贴板 |
| D08 | 切换窗口后不打扰用户 | 停止自动粘贴，结果仍复制；可显示小提示，提示可开关 |
| D09 | 手动热词必须有 | 支持词表编辑和逐次识别生效；自动词表延后 |
| D10 | App 内保留全部转写记录，可删除 | 重启后仍存在，不限制最近 20 条，不自动按时间淘汰 |
| D11 | 为后续 DMG 打包准备代码 | 本轮开发不制作 DMG；模型随包还是独立安装暂不决定 |
| D12 | 实际使用后由用户反馈 | 不以 7 天留存、5 位用户或预先设定的延迟百分位作为完成门槛 |

本表优先于前面讨论中的建议。原先“只做一种快捷键模式”“恢复旧剪贴板”“只存最近 20 条”“现在打包 DMG”“立即制定 99% 注入成功率”等建议不进入首版硬性要求。

### 1.2 首版交付物

- 可在开发机启动、脱离终端主窗口操作的 macOS `.app` 开发构建。
- 菜单栏入口、状态浮层、设置窗口、历史记录窗口。
- 快捷键 → 录音 → 本地 Qwen → 历史存储/剪贴板 → 条件满足时自动粘贴的完整流程。
- 独立 ASR Worker、版本化通信协议、环境与模型路径配置。
- 可复现的开发构建步骤、测试结果、故障排查说明及后续打包检查清单。

### 1.3 不在首版范围内

Windows/Intel Mac、系统 IME/Input Method Kit 集成、LLM 润色、自动生成热词、用户画像、语音命令、边说边转写、会议转录、云同步、账号、远程遥测、自动更新器、正式 DMG、发行签名/公证，以及模型下载器。

历史列表分页、基础搜索和删除属于首版；历史导出、跨设备同步和音频回放不属于首版。

## 2. 用户流程与行为规则

### 2.1 首次启动与日常启动

首次启动检查 Apple Silicon、麦克风授权、全局快捷键监听/自动粘贴所需权限、Python runtime 和本地模型。缺失时显示对应状态和修复入口，不将“正在启动”无限悬挂。

权限必须由 App 正常申请，引导用户在系统设置中操作。不得宣称“一键安装”能够绕过系统授权。实现阶段验证所选事件监听方式是否还需要输入监控权限，仅请求实际使用的权限。

开发版允许通过配置指定项目内 Python 和模型目录。日常启动时后台加载模型，完成就绪检查后进入 Ready；未就绪时不悄悄录音或丢弃用户语音。界面提供重试和重新加载入口。

菜单栏常驻；关闭历史/设置窗口不退出 App，菜单中的“退出”才结束主进程及 Worker。开机启动属于可选便利项，不阻塞核心开发完成。

### 2.2 两种录音模式

| 输入 | 行为 |
| --- | --- |
| 按住 Hold 快捷键 | 开始录音，忽略键盘自动重复事件 |
| 释放 Hold 快捷键 | 结束录音，提交整段音频 |
| 按一次 Toggle 快捷键 | 开始录音 |
| 再按一次 Toggle 快捷键 | 结束录音，提交整段音频 |
| 录音中按 Esc | 取消，不生成历史文本、不改剪贴板、不粘贴 |
| 识别中按 Esc | 标记取消；任何晚到结果都不得入历史、改剪贴板或粘贴 |
| 识别中触发新录音 | 首版不排队、不并发，保留忙碌状态；非阻塞提示可以关闭 |
| 一种模式录音中触发另一种模式 | 忽略，维持当前录音所属模式 |

取消识别后若底层推理不能立即中止，界面显示正在取消；Worker 退出本次任务或被重建后才再次 Ready，不能让两次推理重叠。

快捷键分别持久化。必须拦截 App 内两种快捷键互相冲突，处理修饰键变化、丢失 key-up、事件 tap 被禁用、睡眠唤醒。不能承诺检测所有其他应用的快捷键冲突；注册失败应明确提示。默认键组合在 M0 实测后选定，不把 Fn/单个修饰键的支持当成前提。

### 2.3 录音与识别

- 仅在明确开始录音后采集语音，停止/取消后关闭采集；模型常驻不代表持续监听麦克风。
- 采集设备实际格式，转换成 Qwen 所需的 16 kHz 单声道音频；设备标称格式不能直接当作模型输入格式。
- 首版使用系统默认麦克风，设置页显示当前设备并能重新检测。独立设备选择器可后置，不得阻塞主流程。
- 使用原始 ASR 标点、大小写和措辞；默认仅去除首尾空白。不要全局删除连续标点、改数字或中英文间距，以免误改省略号、技术文本和用户表达。
- 不增加“换行”“下一段”等语音指令解析；这些词按普通语音识别。保留模型本身输出的换行。
- 对空录音、低音量和疑似无语音做保守处理。简单能量检测不等于可靠 VAD，不能保证消除噪声幻觉；低声短词要进入测试。
- 录音上限、推理超时属于资源保护参数，先集中配置并用目标机实测，不是用户已确认的延迟验收标准。

实施默认：录音上限先设 120 秒、推理看门狗先设 120 秒，接近上限时在录音条显示；到录音上限停止并提交整段音频。若无法稳定处理上限长度，应缩小上限并在界面说明，不擅自实现分段。短录音不机械地按 300 ms 截断，要测试“好”“收到”等短词。

### 2.4 剪贴板与自动粘贴

“不论是否成功都在剪贴板里”指：只要得到有效、非取消的转写，不论自动粘贴成功、失败或被跳过，都要写入剪贴板。ASR 失败、空结果和取消没有可输出文本，不清空或替换现有剪贴板。

处理顺序：

1. 校验 request ID、会话状态和结果非空。
2. 尝试持久化历史正文，暂存本次文本；即使存储失败，也继续尝试复制，显示可恢复的存储错误。
3. 将本次文本写入系统剪贴板；失败时仍从历史/本次结果提供“重新复制”。
4. 重新确认目标应用、窗口、输入焦点和剪贴板状态。
5. 条件允许时仅发出一次粘贴操作；不自动按 Enter、不发送邮件或微信消息。
6. 记录复制结果、粘贴尝试或跳过原因，回到 Ready。

从录音开始记录目标应用/窗口/可访问的输入元素。如果录音或识别期间切换过目标窗口，即使后来切回，也跳过本次自动粘贴。相同窗口内更换输入框或出现无法确认的焦点变化，同样跳过。状态浮层不能激活应用、成为 key window 或抢走输入焦点。

首版以“写入 NSPasteboard + 模拟 Cmd+V”为唯一自动输入路线。无需同时开发 AX 直接改全文和逐字键盘注入。AX 用于辅助检查焦点，不用覆盖输入框全文。

自动粘贴前确认触发快捷键的修饰键已经释放；尤其 Toggle 停止时不可在用户仍按着其他修饰键的情况下合成错误组合。确认剪贴板仍是本次写入的版本；若用户随后复制了其他内容，跳过自动粘贴，不能把新内容贴出，也不持续覆盖剪贴板把它“抢回来”。历史中仍能找回本次转写。

“粘贴事件已发出”不能等价于“目标应用已插入文字”。某些应用无法可靠反馈粘贴结果，因此状态/日志使用 `paste_attempted`，仅在人工测试或可靠验证下认定插入成功。不自动重试粘贴，避免重复文本。

密码框、无光标、目标退出、权限不足或焦点无法确认：保留剪贴板与历史，跳过自动粘贴。跨应用兼容性不能以“任意文本框保证可用”表述。

### 2.5 提示与窗口

录音中持续可见状态条和录音时长；识别中显示处理状态，不必展示实时转写或复杂动画。

复制后未自动粘贴时，默认显示短暂、非模态、不抢焦点的小提示：“已复制，可按 ⌘V 粘贴”。用户已经可以粘贴，不再要求重复手动复制。菜单/历史中仍提供复制入口。

设置项“显示结果提示”控制成功、仅复制、忙碌等轻提示，默认开启；关闭后不弹这些提示，但菜单栏状态和必要的录音中标识仍在。权限缺失等阻塞错误保留在菜单/设置页可见，不强制弹模态对话框。默认无提示音。

### 2.6 手动热词

- 支持新增、编辑、删除、启用/停用及总开关；支持按行批量粘贴。
- 支持中文、英文、空格组成的词组，保留用户指定的大小写。
- 去首尾空白、过滤空项、对完全重复项去重；大小写不同的条目提示重复可能，不擅自重写。
- 每次提交 ASR 时对当前启用词表做快照；过程中编辑只影响下次请求。
- 把词表作为 `hotwords` 参数交给 Qwen，不在输出后按同音词无条件替换。
- 词表预算必须同时考虑数量和上下文 token；实现前核对当前 tokenizer/runtime 限制，设有依据的上限并明确显示，不能静默截断。
- 测试无关词和相近词干扰。Qwen 的热词是上下文提示，不保证每次命中，不承诺“添加即必定正确”。

### 2.7 完整转写历史

默认保存全部有效转写正文，包括自动粘贴尝试、仅复制、粘贴失败以及剪贴板写入失败的结果。没有正文的错误、空结果、用户取消不伪造成转写记录。

历史按时间倒序，显示时间、正文预览、录音时长、输出状态，支持查看完整正文、复制、正文关键词搜索、单条删除和清空全部。清空全部需二次确认；单条删除提供明确操作，基础版可不实现撤销。

历史不设条数上限或自动过期，但按页读取，不把全部正文常驻内存。App 重启或 Worker 重启后仍在；每次请求最多一条记录，晚到响应和 Worker 重试不得重复写入。

不保存原始音频，不保存完整目标文档、窗口标题或用户旧剪贴板。开发诊断日志不复制转写正文。历史存储在用户本地应用数据目录，首版不额外承诺应用级数据库加密。

删除记录后不通过重放响应将其恢复；数据库删除只承诺不再出现在应用查询中，不宣称取证级擦除。删除历史不主动清空用户系统剪贴板；清空历史不删除热词和设置。

## 3. 页面与设置清单

| 界面 | 必需内容 |
| --- | --- |
| 菜单栏 | Ready/录音/识别/错误状态；打开历史；设置；重试加载；退出 |
| 状态浮层 | 非激活窗口、录音状态和时长、识别状态、可关闭的轻提示 |
| 快捷键设置 | Hold 与 Toggle 两个录制控件、冲突提示、恢复默认 |
| 热词设置 | 列表、增删改、条目开关、批量粘贴、总开关 |
| 通用设置 | 结果提示开关；当前默认麦克风；权限检测入口 |
| 模型/诊断 | 模型就绪状态、配置路径、重新加载、日志位置、错误信息 |
| 历史窗口 | 分页列表、全文查看、复制、搜索、单删、清空 |

模型安装方式、发布签名、模型下载 UI、复杂主页不作为首版页面。

## 4. 当前工程基础与技术选择

### 4.1 已有证据

- 项目使用 uv 和 Python 3.11，各模型环境独立。沿用 `环境搭建指引.md`，避免引入全局 Python 依赖。
- `benchmark/backends/run_qwen_mlx.py` 已验证 `load_model(model_dir)` 与 `model.generate(audio, temperature=0.0, hotwords=...)`，返回 `result.text` 和 `result.language`。
- `envs/mlx/pyproject.toml` 管理 `mlx-audio[stt]`；模型位于 `models/qwen3-asr/Qwen3-ASR-0.6B-4bit`。
- 本地已安装 Qwen 实现的 `sample_rate` 是 16000，支持 `stream=False`。库内有长音频切块逻辑，当前默认 `chunk_duration=1200` 秒。必须锁定依赖并验证首版录音上限以内不触发内部切块；不能只因为 App 没有分段代码就宣称完全未分段。
- 当前 benchmark：10 条、单一录音人、A18 Pro/8GB；Qwen 基础 MER 2.64%，热词模式 2.26%。约 1415 MB 是 MLX Metal 峰值口径，不等于应用总内存，也不能与进程 RSS 简单相加。

选型足以开始开发，但不作为跨应用粘贴、后台稳定性和总内存占用已通过验证的证据。

### 4.2 实施默认技术栈

| 层 | 选择 | 责任 |
| --- | --- | --- |
| macOS App | Swift + SwiftUI + AppKit | 菜单栏、设置、历史、非激活浮层、生命周期 |
| 系统交互 | AVAudioEngine/AVAudioConverter、Core Graphics、Accessibility、NSPasteboard | 录音/转换、快捷键、焦点检查、剪贴板和粘贴 |
| ASR Worker | Python 3.11 + 已锁定的 MLX/mlx-audio | 加载 Qwen、串行推理、结构化结果 |
| 本地通信 | 主进程启动子进程，stdin/stdout JSON Lines | 单一父子进程通信，无需本地 HTTP 服务 |
| 数据 | SQLite + UserDefaults | 历史/热词与轻量设置 |
| 构建 | Xcode App target，核心逻辑可拆 Swift Package | 开发 `.app`、测试、后续打包的资源入口 |

这是实施方案，不是用户点名选择的技术栈。优先做 M0 验证；若工具链或系统权限暴露问题，在实施记录中说明并调整。不要因 UI 框架比较延后核心链路。

### 4.3 模块边界

```text
MenuBar / Settings / History / HUD
                  │
          SessionCoordinator
          ├── ShortcutService
          ├── PermissionService
          ├── AudioCaptureService
          ├── FocusTracker
          ├── ASRClient → WorkerSupervisor → Python Qwen Worker
          ├── HistoryRepository / VocabularyRepository
          └── OutputService → Clipboard + conditional Cmd+V
```

`SessionCoordinator` 是唯一会话状态所有者；UI 只发送用户意图并订阅状态。录音、焦点、推理、剪贴板和存储通过接口注入，测试可替换，不把模型调用塞进视图或键盘回调。

ASR 常驻加载，首版不实现闲置卸载策略；录音、IO 和推理不得阻塞主线程或事件 tap 回调。模型是否需要预热、预热耗时与驻留内存在 M0 实测，预热不得生成用户历史或写剪贴板。

## 5. 会话、协议与故障恢复

### 5.1 状态机

```text
Starting → Ready → Recording → Transcribing → Delivering → Ready
              ↑        │              │
              └─取消───┘              └→ Cancelling → Ready / Recovering
Starting / 任意活动状态 → Blocked（权限、配置）或 Recovering（Worker）
Blocked / Recovering → 检测/恢复成功 → Ready
```

每次会话包含 UUID、触发模式、目标焦点标记、目标是否曾改变、录音起止、取消标记和 Worker generation。回调必须匹配当前 request ID 与 generation；退出、取消、超时之后的旧结果一律丢弃。

历史输出状态与会话状态分开：`clipboard_written` / `clipboard_failed`，`paste_attempted` / `paste_skipped` / `paste_failed`。跳过原因单独存储，如 `focus_changed`、`focus_unknown`、`permission_missing`、`clipboard_changed`。

### 5.2 Worker 协议 v1

主进程使用明确的 executable 与 argument 数组启动 Python，不经 shell 拼接路径。开发模式可解析项目内 `.venv`；正式运行路径留给 RuntimeLocator，不能散落硬编码。

初始化参数指定模型目录；Worker 在成功加载及必要预热后返回：

```json
{"v":1,"type":"ready","model_id":"qwen3-asr-0.6b-mlx-4bit","sample_rate":16000}
```

请求示例：

```json
{"v":1,"type":"transcribe","request_id":"uuid","audio_path":"/absolute/session/uuid.wav","hotwords":["Paimon","FastAPI"],"language":null}
```

响应示例（数值仅说明格式）：

```json
{"v":1,"type":"result","request_id":"uuid","text":"明天整理笔记。","language":"Chinese","inference_ms":1234}
```

```json
{"v":1,"type":"error","request_id":"uuid","code":"INFERENCE_FAILED","message":"本地识别失败","retryable":true}
```

- UTF-8、一行一个完整 JSON；转写中的换行必须 JSON 转义，不破坏分帧。
- stdout 仅协议，第三方输出重定向到 stderr；父进程持续排空 stdout/stderr，防止管道塞满。
- 检查协议版本、消息尺寸、必填字段和路径；音频仅接受 App 当前会话临时目录内的文件。
- 首版串行处理单请求；一个模型实例；设置加载和推理看门狗，值集中配置并测后调整。
- MLX 推理未必可协作取消，首版由主进程取消会话并按需终止/重建 Worker；不谎称发一个 cancel JSON 就能中断阻塞推理。
- 崩溃后有界退避重启（实施默认最多连续 3 次），持续失败进入可见错误状态，避免重启循环。
- 不自动重放已经输出过的请求；退出时清理子进程，父进程管道 EOF 后 Worker 退出。
- 不在异常日志中打印正文、热词全文和完整用户文件内容。

### 5.3 音频文件生命周期

使用 App 私有会话临时目录，以 UUID 命名；录音完成并关闭文件后再交 Worker。结果完成、取消且 Worker 不再读取、超时或退出后清理音频。启动时清理本应用遗留的已结束会话临时文件，不递归清理通用临时目录。

### 5.4 本地数据模型

建议 SQLite 表：

| 表 | 核心字段 |
| --- | --- |
| `transcriptions` | `id`（request UUID，主键）、`created_at`、`text`、`audio_duration_ms`、`inference_ms`、`end_to_end_ms`、`model_id`、`hotwords_enabled`、`hotword_count`、`clipboard_status`、`paste_status`、`skip_reason` |
| `hotwords` | `id`、`text`、`enabled`、`created_at`、`updated_at` |
| `schema_version` 或 PRAGMA user_version | 数据库版本与顺序迁移 |

设置存 UserDefaults：两组快捷键、提示开关、热词总开关、开发环境路径覆盖等。不要把 Python 解释器路径或模型路径写进每条历史。

数据库在串行后台执行器中访问，正文先落盘、输出状态后更新；主键保证单请求不重复。分页使用时间与 ID 游标。基础搜索可先参数化查询，数据量增长后再引入全文索引，避免首次实现过多基础设施。

数据库写入失败时保留本次结果、继续复制并允许重试保存；不能宣称已保存。数据库损坏时不自动删除原库重建。历史删除与晚到状态更新要协同：状态更新只 UPDATE，不 UPSERT 已删除正文。

数据路径：`Application Support/<bundle-id>/`；日志使用应用日志目录；临时音频使用应用自己的临时子目录。模型和 runtime 只读，用户数据不写 `.app` 内，不依赖当前工作目录。

## 6. DMG 打包准备的边界

这次应用实施要准备好以下条件，但不创建正式 DMG：

1. 有正常 `.app` target、固定开发 Bundle ID 和权限用途说明，稳定 App 身份便于反复测试系统授权。
2. `RuntimeLocator` 隔离开发 Python 与未来打包 runtime；`ModelLocator` 隔离外部模型目录与未来 bundle/安装目录。
3. runtime/model 的 ID、版本、依赖锁和资源清单有出处，加载不依赖联网解析远端模型名。
4. 用户数据与 bundle 分离；测试从非仓库工作目录启动，验证空格/中文路径。
5. 保留 packaging 文档，记录嵌入 Python、MLX 动态库/Metal 资源、签名、公证和许可证核对的未来验证项。

不能把复制开发机 `.venv` 等同于可分发运行环境；未来须在无开发环境机器上验证。现在不要求用户安装下载器，也不因后续 DMG 考虑直接重写 Python 推理为 Swift。

后续发行时再决定：模型是否随包、外部模型如何安装、下载源与校验、Developer ID/公证、正式 App 名称和最低支持系统版本。首版只承诺当前目标机器验证过的系统，不凭技术栈推断兼容所有历史 macOS。

## 7. 分阶段实施与验收

依赖顺序：M0 → M1 → M2 → M3 → M4。按可运行成果推进，不绑定“几周必定完成”的承诺。每阶段记录实现、实测、未验证项和下一步。

### M0：验证系统交互和现有 ASR 接口

任务：建立最小原生 App 开发 target；确认构建工具链；验证两种快捷键的按下/释放事件、非激活浮层、录音权限及 16 kHz 转换；用固定测试文字验证剪贴板与自动粘贴；把现有 Qwen 调用跑成常驻进程原型。

输出：最小开发 App、系统能力验证记录、可用默认快捷键组合、runtime/model 路径解析草案。复用现有模型，不重新下载或重跑全模型选型。

验收：在用户指定/已安装的 Office、浏览器、Mail、微信输入位置人工确认粘贴；不存在的应用记为未验证。事件发出不冒充真实插入。固定文字测试须使用专门测试输入框，不触发实际发送或改写用户文档。

### M1：完整端到端输入链路

任务：实现 SessionCoordinator、两种模式、Esc、录音、Worker 协议和监管；有效文本写入最小历史存储与剪贴板；焦点检查通过后自动粘贴；建立基础菜单栏与状态条。

输出：可以用语音输入真实文本的开发 App，完整历史正文已落盘，界面可暂时简陋。

验收：两种方式各完成连续多次录音；取消/空结果不触碰剪贴板；切窗口后仅复制；从语音到粘贴全程无需切回 App；不自动按 Enter；退出不留 Worker。

### M2：手动热词、历史和用户设置

任务：实现完整热词编辑和请求快照；历史列表/搜索/复制/删除/清空；快捷键自定义；提示开关；模型/权限诊断入口；设置和历史跨启动持久化。

验收：词表改变在下一次识别中生效；历史包含所有非取消有效结果且无重复；删除后重启不恢复；提示关闭不再弹轻提示；两种快捷键配置独立保存。

### M3：异常恢复与真实应用兼容

任务：完成焦点变化、剪贴板竞争、设备变化、睡眠唤醒、权限撤销、Worker 崩溃/超时、数据库写入失败和长时间运行的恢复；测短词、静音、噪声、最长允许录音。

验收：见第 8 节检查表。无法自动粘贴时应留下可访问的文本；不可宣称任意应用 100% 可粘贴。UI 不冻结，无失控重启或多 Worker/录音实例。

### M4：开发交付与后续打包准备

任务：整理启动/构建脚本和开发文档；清理硬编码；验证非仓库工作目录启动；记录依赖、模型版本和数据位置；完成未来 packaging 清单；汇总实测延迟和资源。

验收：用户可启动开发 `.app` 自用；现有 benchmark 仍可运行；开发步骤可复现；交付不依赖当前终端的偶然环境变量。此阶段不承诺无开发环境机器可用，也不制作 DMG。

## 8. 验证方案

### 8.1 自动化验证

- Swift 核心测试：Hold/Toggle 状态迁移、键盘重复事件、交叉模式、取消及晚到结果、焦点改变、单请求仅输出一次。
- OutputService 使用替身：有效结果必尝试复制、复制失败不粘贴、焦点改变不粘贴、剪贴板被替换不粘贴、不得自动重试或发送 Enter。
- 存储测试：迁移、分页/搜索、重启后读取、重复 request ID、单删/清空、晚到状态更新不复活删除记录、写入失败可恢复。
- Worker 协议测试：UTF-8/换行、噪声日志隔离、坏消息、启动失败、超时、EOF、崩溃恢复、旧 generation 响应。
- 默认测试用 mock ASR，不要求每次加载真实模型；单独运行 Qwen smoke test 验证真实音频、热词参数和输出。适当运行现有 benchmark 回归，不扩展为新一轮模型海选。

### 8.2 人工系统验收矩阵

| 场景 | 预期 |
| --- | --- |
| Office 文档/输入区域、浏览器文本框、Mail 正文、微信编辑框 | 正文可粘贴，保留合理的原 ASR 文本；不自动发送 |
| 同应用切窗口、跨应用切换、同窗口换输入框 | 能确认变化时跳过自动粘贴；无法确认时保守跳过 |
| 结果产生后用户复制其他内容 | 不将其他内容自动粘贴，不循环覆写剪贴板；历史仍可复制 |
| Hold 松手、Toggle 再按、录音中 Esc | 分别正确结束或取消，不丢 key-up 后永久录音 |
| 识别中 Esc 后结果晚到 | 结果不进入历史或剪贴板，不粘贴 |
| 两个快捷键冲突、事件监听失效 | 可见错误和恢复路径，不假装 Ready |
| 麦克风/辅助功能权限拒绝与撤销 | 识别权限缺失阻止录音；只有粘贴权限缺失时可仅复制（前提是快捷键仍可用） |
| 蓝牙/USB 麦克风断开、系统睡眠唤醒 | 结束或取消受影响会话，恢复后可再次输入，不错采样率 |
| Worker 加载失败、崩溃、超时 | 有界恢复，无无限等待；旧请求不重放 |
| 静音、噪声、单字、不同录音长度 | 记录真实行为，不夸大能量门限能解决幻觉 |
| 数据库不可写、剪贴板写入失败 | 独立报告失败，结果保留在可访问位置，不谎报保存/复制成功 |
| 持续使用、历史大量增长、App 重启 | 历史完整持久、分页不冻结、资源不无界增长 |
| 非仓库工作目录、中文/空格路径 | 仍能定位 runtime/model/data，不依赖 shell 工作目录 |

每条测试记录应用/系统版本、步骤和结果。没有安装的 Office 或没有真实麦克风权限时，写“未验证及原因”，不得用 mock 测试代替真实兼容性结论。

### 8.3 性能记录，不设预先硬门槛

使用单调时钟记录：按下到开始录音、松开到音频准备完毕、推理耗时、结果到剪贴板、结果到粘贴事件、松开到剪贴板/粘贴事件。系统无法确认目标渲染时，不将事件时间叫作“用户看到文字的时间”。

区分首次加载、首次推理和热启动；测量 App 与 Worker 各自 RSS、MLX Metal 内存指标、系统内存压力、空闲 CPU 和连续调用后的资源趋势。不同内存口径不得直接相加或混排。

结果供用户自用后判断；当前不以 P95 ≤ 2 秒、固定准确率、用户数量、修改比例或留存作为交付门槛。记录无需上传，不监听用户后续打字来计算“修改率”。

## 9. 建议目录与 Codex 实施约束

```text
apps/macos/                  原生 App target、UI、系统适配与 Swift 测试
voice_input/                 Python Worker、协议、Qwen adapter
tests/                       保留已有 benchmark 测试，新增 Worker 测试
docs/                        开发启动、兼容性矩阵、实施记录、packaging 准备说明
scripts/                     新增 App 开发构建/启动辅助脚本
envs/mlx/                    复用 Python 3.11 / uv 管理的 MLX 环境
benchmark/                   保留现有评测工具
models/                      复用当前本地模型
```

后续 Codex 按以下约束执行：

1. 先读取 `AGENTS.md`、本文件、`环境搭建指引.md` 和现有 Qwen backend，再从 M0 开始。
2. 保留已有 benchmark、评测结果、模型文件和用户改动；不要为 App 开发大范围整理或删除它们。
3. 可以把推理逻辑封装为 adapter，但不要直接复用 benchmark CLI 的 manifest 批处理循环作为产品 API。
4. Python 使用 uv/Python 3.11，保留依赖锁；新增 App 构建要求写入开发文档。不要在运行时调用 `uv sync` 或联网安装依赖。
5. 每阶段留下可运行产物和验证记录；本地代码测试通过与真实系统交互验证必须分开报告。
6. 依照 D01–D12 实现；不要自行添加 LLM、分段、自动清理历史、剪贴板恢复、现在制作 DMG 或产品留存要求。
7. 技术细节可在既定范围内调整并说明；改变用户行为或扩展发行范围时，先列明具体影响再确认。
8. 交付说明包含实际完成项、验证环境、启动入口、数据位置和剩余限制，不把设计目标写成已经实现的能力。

## 10. 待决事项与解决时机

| 事项 | 处理时机 | 是否阻塞启动开发 |
| --- | --- | --- |
| 默认快捷键组合、是否支持单修饰键 | M0 验证后选定；用户随时可自定义 | 否 |
| 支持的最低 macOS 版本 | M0 记录开发目标，发行前补测确定 | 否 |
| 热词上下文预算、录音/推理保护值 | M0–M3 根据当前依赖和目标机实测调整 | 否 |
| 热启动延迟和内存是否令人满意 | 用户实际使用后反馈 | 否 |
| 模型随 DMG、外置安装还是下载 | 正式打包阶段由用户决定 | 否 |
| 嵌入 Python 的具体分发方式、签名和公证 | 后续发行验证 | 否；须保留路径/资源边界 |
| 最终产品名、图标、正式 Bundle ID | 发行前确定；开发期间身份保持稳定 | 否 |

## 11. 依据与参考

项目依据：`产品立项文档.md`、`results/ASR_Benchmark报告.md`、`环境搭建指引.md`、`benchmark/backends/run_qwen_mlx.py`、`envs/mlx/pyproject.toml` 及本次用户问答。上述已确认需求与旧建议冲突时，以本文件 D01–D12 为准。

技术参考入口（具体可用性须在实现目标系统核对）：

- [Apple AVAudioEngine](https://developer.apple.com/documentation/avfaudio/avaudioengine)：音频节点和录音处理链路。
- [Apple NSPasteboard](https://developer.apple.com/documentation/appkit/nspasteboard)：系统剪贴板读写。
- [Apple NSPasteboard.changeCount](https://developer.apple.com/documentation/appkit/nspasteboard/changecount)：检测剪贴板所有权/内容版本是否发生变化；不提供跨应用原子粘贴保证。
- [Apple CGEvent tapCreate](https://developer.apple.com/documentation/coregraphics/cgevent/tapcreate(tap:place:options:eventsofinterest:callback:userinfo:))：全局事件 tap 的创建与失败处理；权限和快捷键行为由 M0 实测。
