# 言落（VoxDrop）macOS 离线 DMG 打包与发布实施方案

日期：2026-09-12。

状态：实施中。P0–P5 的代码、构建、离线资源、签名、DMG 和本机自动验证已经完成；P6 的另一台 M 系列 Mac 与权限人工验收尚未完成，P7 的 GitHub Release 文件已准备但尚未上传。

当前候选：`dist/VoxDrop-0.1.0-macos-arm64.dmg`，710,724,230 字节，SHA-256 为 `ffff40da47bb4186b40068a4a77d36efd26d455d8f0410d5e4b4e0e6770770fd`。已从只读 DMG 复制到随机目录并推出镜像，使用复制品的包内 runtime/model 完成真实离线中文识别。仍不能把这项本机结果替代另一台干净 Mac、Gatekeeper 和三项权限的人工验收。

## 1. 已确认的交付要求

| 项目 | 本次要求 | 完成判定 |
| --- | --- | --- |
| 硬件 | 其他 M 系列芯片 Mac 可运行，arm64 原生执行 | 在独立 M 芯片 Mac 完成安装、录音、识别、粘贴；不依赖 Rosetta |
| 系统 | macOS 26.2 及以上；用户已明确不需要兼容 macOS 13 | App 声明、Swift 构建目标、所有原生依赖的最低版本一致；最低系统实机测试通过 |
| 运行依赖 | 用户无需安装 Python、uv、Homebrew、Xcode、命令行工具、ffmpeg 或其他组件 | 下载后按说明安装并授权即可使用 |
| 模型 | 已验证的 Qwen3-ASR-0.6B MLX 4-bit 随 App 分发 | 权重、tokenizer、配置及必要资源全部在 App 内 |
| 网络 | 首次加载和日常识别均不需要联网 | 无模型缓存、断网情况下完成首次真实识别 |
| 安装包 | 一个压缩 DMG，严格小于 1 GB | 最终文件大小 `< 1,000,000,000` 字节；按十进制 GB 验收 |
| 安装后体积 | 不设硬性上限 | 不为缩小安装目录牺牲功能和稳定性 |
| 签名 | 没有 Developer ID，接受未认证/未公证警告 | 使用 ad-hoc 签名；真实下载后的安装与允许打开流程可用 |
| 质量 | 尽可能减少安装、启动和使用故障 | 发布必过检查全部完成，未解决的阻断问题不能带入公开版本 |

“无需依赖”指无需用户额外安装软件组件，仍需要 macOS 的麦克风、输入监控、辅助功能授权，以及未公证应用的系统确认。受企业管理策略限制的设备不能承诺绕过管理员政策。

发布目标为符合上述系统要求的 M 系列 Mac。不能仅凭 arm64 编译成功就声称所有芯片、所有系统小版本都已验证；实际测试设备和已知限制必须写入发布记录。

## 2. 方案原则与范围

1. 保留 Swift App + Python Worker + MLX + 当前模型的技术路线，优先完成自包含发行。
2. 固定现有模型权重、量化方式和识别参数，不通过更换模型、再次量化、缩短录音上限或减少热词能力来满足包体积。
3. 先构造完整可用的运行环境，再测真实 DMG；只有超出预算时才逐项裁剪。
4. 开发路径和发行路径明确区分；发行版资源缺失时报告损坏，不能悄悄回退到开发机或联网下载。
5. 一项改动对应一组验证；资源搬迁、依赖裁剪、进程恢复分别实施，便于定位回归。
6. 发行包经过测试后保持字节不变；上传同一份已验收文件。
7. 本轮不要求首次启动下载器、自动更新器、云服务、账号、Intel/Windows 支持或性能架构重写。
8. 对已经发现的生命周期风险做针对性加固和故障测试。不能以“打包”为由顺手改变粘贴规则、热词提示方式或录音交互。

本文列出的发行脚本、Bundle 配置和自动检查现已落地；带“未验证”的跨设备和人工测试仍不能视为完成。

## 3. 当前代码与资源检查结果

### 3.1 现有启动链路

```text
scripts/run-macos-app.sh
  → 构建 debug App
  → 把开发机项目目录写入 UserDefaults
  → AppModel.launch 读取 projectPath
  → RuntimeConfiguration.development
  → 项目内 .venv/bin/python -m voice_input
  → 项目内模型目录
  → 录音 WAV → Worker 识别 → 历史/剪贴板/粘贴
```

当前 App 本体可启动不等于可独立分发。发行资源解析必须覆盖启动、录音、清理、重试和诊断显示的全部调用点。

| 位置 | 已确认事实 | 实施要求 |
| --- | --- | --- |
| `scripts/build-macos-app.sh` | 只构建 debug，复制二进制、图标、Info.plist，并写开发项目路径文件 | 新增正式 release 构建与完整资源组装流程 |
| `scripts/run-macos-app.sh` | 通过 `defaults write` 配置项目目录 | 普通用户启动不得依赖此脚本 |
| `AppModel.launch` | `projectPath` 为空就阻止启动；仅创建 development Worker | 新增正式发行配置，发行版忽略遗留项目路径 |
| `RuntimeLocator` / `ModelLocator` | 只知道仓库中的 Python 和模型位置 | 使用当前 App Bundle 推导绝对资源路径 |
| `WorkerClient.start` | 工作目录为项目根目录，启动环境未经隔离 | 使用包内解释器、包内 Worker、受控环境 |
| `AppModel.begin` / `cleanupStaleSessions` | 再次直接创建 development 配置 | 与 Worker 共用同一份 sessionDirectory 配置 |
| 设置与诊断页面 | 显示“项目目录”，模型路径由项目路径拼接 | 发行版移除项目目录输入，显示实际内置资源状态 |
| `envs/mlx/.venv/bin/python`、`pyvenv.cfg` | 含开发机绝对路径 | 重新组装可迁移 runtime，不原样复制 `.venv` |
| `config/runtime-resources.json` | 已记录模型两个摘要和依赖锁摘要，但不是完整发行验证机制 | 增补完整来源、文件清单、平台和版本，并让构建/验证程序使用它 |
| `AppIdentity` / SQLite | 用户数据库已经放在 Application Support，SQLite 使用 WAL | 保持用户数据独立于 App；补覆盖更新和失败恢复验证 |

### 3.2 系统版本与依赖基线

本次检查的依赖基线：

| 组件 | 版本/信息 |
| --- | --- |
| Python | 3.11.15，发行时固定来源及构建版本 |
| MLX / mlx-metal | 0.32.2 |
| mlx-audio | 0.5.3 |
| Transformers | 5.17.0 |
| NumPy | 2.4.6 |
| SciPy | 1.17.1 |
| 依赖锁 | `envs/mlx/uv.lock` |
| 锁文件 SHA-256 | `b211ac44a4f1fd18b8fed0f9848c1f5be0e11d1f654307a551bcd8c2aaa41090` |

本地 MLX wheel 标记为 `macosx_26_0_arm64`，但 `mlx/core*.so` 与 `mlx/lib/libmlx.dylib` 的 `LC_BUILD_VERSION` 实际显示 `minos 26.2`。必须看二进制和真实运行结果，不能只看 wheel 文件名。

现有 Swift 二进制和 Info.plist 声明的最低 macOS 13 不代表整个 Python/Metal 链路兼容 macOS 13。用户已确认最低 macOS 26.2，本方案沿用当前依赖路线，不为支持旧系统降级依赖。

实现时统一更新：

- `apps/macos/Resources/Info.plist` 的 `LSMinimumSystemVersion` 为 `26.2`。
- SwiftPM 平台/部署目标设为 macOS 26.2，并验证最终 Mach-O 的真实最低版本。
- README、使用说明、版本清单和 Release 文案全部写明 macOS 26.2+、Apple Silicon。
- 扫描所有随包 Mach-O 的 arm64 slice 与最低系统版本；不允许某个隐藏依赖实际要求高于 26.2。

前次检查中 Python 11 项测试、Swift 核心检查和使用项目模块缓存的 release 编译通过。这是开发代码检查结果，不是正式发行包验收。初次直接编译出现的 SDK/编译器错误在使用项目缓存后未再出现，不能据此认定 toolchain 已损坏。正式构建仍需固定并记录 Swift、SDK、Xcode/Command Line Tools 版本。

计划编写期间工作区出现权限设置入口的未提交修改。实施前重新记录工作区状态，保留已有改动，不把较早测试结果直接归属于之后修改过的源码。

### 3.3 模型与体积证据

- 模型来源：`mlx-community/Qwen3-ASR-0.6B-4bit`。
- README 固定 revision：`313d850181767edf09f00a9c289becca70e58cd0`。
- 权重文件：`708,236,945` 字节，整个目录约 680 MiB。
- `config.json` SHA-256：`923618cf5ca452fda0253a6be5c1a17f94a2e4851d3b98beb45848565587bd72`。
- `model.safetensors` SHA-256：`70c7e67e588062adce4f10796e47ad42ead51c6671eda61a0987eae38ca95ddf`。
- 上述两个摘要已与本地文件比对一致。其余必需文件仍需补齐摘要。

前次 tar + Zstandard 压缩试验得到：模型约 `596,092,397` 字节，开发 MLX 环境 + Python + Worker 约 `104,973,951` 字节。两者合计约 701 MB（十进制）。这只能说明压缩空间可观，不是 DMG 成品大小；运行环境重组、签名、文件系统和压缩算法都会改变结果。

因此将 700–850 MB 作为工程估算，950 MB 作为内部预警线，严格小于 1,000,000,000 字节作为最终验收线。最终只能用实际生成的 DMG 验收。

## 4. 目标运行与安装链路

```text
用户下载完整 DMG
  → 挂载，拖入 Applications
  → 从已安装的言落.app 启动
  → 按系统提示允许打开未公证应用
  → 检查 macOS / 架构 / 包内资源
  → 引导麦克风、输入监控、辅助功能授权
  → 启动包内 Python Worker
  → 离线加载包内权重、tokenizer、特征提取器
  → Ready
  → 快捷键录音 → 16 kHz 单声道 WAV → 本地识别
  → 历史保存 → 剪贴板/一次粘贴 → 清理临时音频
```

授权不齐时显示缺失项，不承诺未经授权就进入 Ready。模型加载、权限状态和资源错误分别显示，避免用户把权限问题误认为下载或模型损坏。

### 4.1 App 内容与目录策略

发行版所有必要软件组件都必须在 `.app` 内。首先采用独立 CPython 的原有相对目录结构做可迁移验证，逻辑布局如下：

```text
言落.app/
└── Contents/
    ├── Info.plist
    ├── MacOS/
    │   └── VoxDrop
    ├── Resources/
    │   ├── VoxDrop.icns
    │   ├── runtime/
    │   │   ├── bin/python3.11
    │   │   └── lib/python3.11/
    │   │       ├── 标准库与原生扩展
    │   │       └── site-packages/
    │   │           ├── voice_input/
    │   │           └── 锁定的 MLX/Transformers 等运行依赖
    │   ├── models/Qwen3-ASR-0.6B-4bit/
    │   ├── licenses/
    │   └── runtime-resources.json
    └── _CodeSignature/
```

这里选择保持 CPython 与依赖的相对布局，以减少路径改造。需要注意：`Resources/runtime` 中包含原生代码，不属于 Apple 推荐的标准嵌套代码位置；它可能按普通资源封存，不能依赖外层 `codesign --deep` 自动发现并处理全部内容。

阶段一必须验证该布局的逐文件签名、整体封存、只读运行和带 quarantine 的真实安装。若不通过，改用符合标准结构的 Helper/Framework 布局，统一修改资源解析器并重做验证；不能通过忽略签名错误交付。最终路径在这一步冻结，随后所有脚本和 App 使用同一约定。标准嵌套目录与签名顺序参考 [Apple TN2206](https://developer.apple.com/library/archive/technotes/tn2206/_index.html)。

模型使用普通解压文件放在包内，由 DMG 做传输压缩。启动时不再解压一份模型，不向 App 内安装额外组件，避免双份占用、首次解压失败和签名改变。

### 4.2 统一 RuntimeConfiguration

增加开发配置和发行配置两条明确入口。建议配置包含：

- `pythonExecutable`：包内解释器的绝对 URL。
- `modelDirectory`：包内完整模型目录。
- `workingDirectory`：明确存在、无需写入的工作目录。
- `sessionDirectory`：本实例受控临时音频目录。
- `cacheDirectory`：用户可写缓存目录。
- `resourceManifest`：资源版本与完整性清单。
- `mode`：development / bundled，用于区分行为和诊断。

发行入口从当前 `Bundle.main` 推导路径，并验证最终解析仍在预期 Bundle 内。不能用当前工作目录、固定 `/Applications` 路径、UserDefaults 的项目路径或开发机环境变量定位程序资源。

`AppModel` 持有一次解析后的配置，启动、录音、Worker 参数、重试、清理、诊断显示均使用它。发行资源缺失时只提示重新安装和诊断入口，不自动选择仓库路径。

开发版继续可使用项目目录，但只能由开发入口显式开启。发行版遇到遗留 `projectPath` 设置时忽略它，避免开发机测试“误通过”。

## 5. 独立 Python runtime 的构建方案

### 5.1 构建方法

1. 使用固定版本、固定来源、固定摘要的 CPython 3.11 arm64 可迁移发行物。可优先验证 uv 当前使用的独立 Python 来源，但必须记录准确构建版本和归档摘要。
2. 解包到新的 staging 目录；不使用指向开发机 Python 的 `.venv`，不把开发 `.venv` 当成发行 runtime。
3. 根据 `envs/mlx/uv.lock` 在构建机准备所需 wheel，记录实际选中的 wheel 文件、平台标签和 SHA-256。
4. 由构建机上的 uv 将依赖安装到 staging 的独立 Python 环境；所有安装目标必须显式指定，禁止写入系统 Python 或开发环境。
5. 把 `voice_input` 安装到该 Python 的 `site-packages` 中，保证隔离模式下 `-m voice_input` 可导入。
6. 保留包的 `.dist-info`、必要数据文件、许可证、MLX Metal 资源与原生库。动态导入和包版本查询可能依赖这些内容。
7. 核对依赖集合和锁文件；构建中不执行无版本约束的升级。若确实需要补依赖，先更新锁与清单，重新验证。
8. 在另一个带中文和空格的目录中运行 staging runtime，验证 `sys.executable`、`sys.prefix`、`sys.path` 和实际模块路径均不指向源码仓库或用户包。

“移到另一个目录还能 import”是中间检查。完成条件仍然是隔离环境中的真实音频推理以及独立 M 芯片机器验收。

### 5.2 Python 启动隔离

推荐在独立解释器确认可自定位后，用参数数组启动：

```text
<包内 python3.11> -I -B -u -m voice_input
  --model-dir <包内模型绝对路径>
  --session-root <本实例临时目录绝对路径>
```

- `-I` 排除当前目录和用户 site-packages，并忽略 `PYTHON*` 环境变量；因此不能再依赖 `PYTHONPATH` 注入 Worker，也不能依赖 `PYTHONHOME` 修复 runtime。
- `-B` 禁止运行时写入 Python 字节码；不能只设置会被 `-I` 忽略的 `PYTHONDONTWRITEBYTECODE`。
- `-u` 明确启用无缓冲通信，继续保留 Worker 的 JSON Lines stdout 隔离。
- 若所选 Python 必须通过 `PYTHONHOME` 才能运行，应先解决可迁移布局或设计受控启动器，不能把相互冲突的启动参数写进发行版本。

参数行为参考 [Python 3.11 命令行说明](https://docs.python.org/3.11/using/cmdline.html)。

Swift `Process` 采用明确构造的子进程环境，移除开发项目变量、用户注入的 Python/dyld 路径和模型下载配置。保留必要的系统、临时目录和 locale 信息，设置 UTF-8 行为，避免中文路径及协议编码依赖终端配置。PATH 不包含 Homebrew、pyenv 或项目目录；正常使用不能需要从 PATH 找到外部命令。

### 5.3 离线约束与音频解码

- 模型、tokenizer、feature extractor 全部传包内的绝对本地路径。
- 在依赖导入前设置 `HF_HUB_OFFLINE=1`；按锁定版本核实 Transformers 的离线行为和必要配置。
- 设置支持的遥测禁用项，例如 `HF_HUB_DISABLE_TELEMETRY=1`；用户缓存指向本应用目录。
- 缺少任何必需模型文件时明确失败，不尝试远端模型名、登录或下载。
- 在断网、空模型缓存条件下验证第一次加载和第一次推理。环境变量只是约束，真实运行才是证据。[Hugging Face 离线环境变量](https://huggingface.co/docs/huggingface_hub/package_reference/environment_variables)

当前 `mlx_audio.utils` 顶层直接导入 `huggingface_hub.snapshot_download`，Transformers 也依赖 Hub 功能。即使永不下载，仍可能需要这些 Python 包；不能直接删除 `huggingface_hub`。

当前产品由 Swift 生成 16 kHz 单声道 PCM WAV；已安装 mlx-audio 的 WAV 读取使用 miniaudio，部分其他格式才会调用 ffmpeg。首版围绕产品 WAV 链路验证无需 ffmpeg，不新增任意音频文件导入功能。保留 miniaudio 及实际所需原生库，并用不含 ffmpeg/ffprobe 的 PATH 完成真实识别；若所选依赖仍调用外部程序，先修复或内置必要组件，不能让用户安装。

### 5.4 可迁移性检查

发行构建自动扫描：

- 符号链接：不允许指向开发机、uv 缓存或 App 之外的 Python/模型资源；相对链接必须有效。
- `pyvenv.cfg`、`.pth`、入口脚本 shebang、运行配置：不允许存在有效的开发机绝对路径引用。
- Mach-O：所有实际执行组件都包含 arm64；允许系统库路径和正确解析的 `@rpath` / `@loader_path` / `@executable_path`。
- 动态库依赖：不能依赖 `/opt/homebrew`、`/usr/local`、开发账户或构建目录；递归确认包内依赖存在。
- 最低系统版本：检查所有原生依赖，而不是只检查 Swift 主程序。
- Metal：保留 `.metallib` 等真实资源并验证 M 芯片上的 Metal 推理；开发机缓存不能掩盖缺失。

普通文本中的源码来源或调试记录不等同于运行依赖，扫描报告应区分两者；有效路径依赖必须消除，构建机信息也应尽量从公开产物移除。

## 6. 模型与依赖完整性

模型最初采用完整部署文件集合，至少覆盖权重、索引、`config.json`、tokenizer 配置、词表、merges、预处理配置及现有模板等文件。不要仅凭文件名判断“似乎没用”就删除。

发行清单记录：

- App 版本、build number、源码提交及构建输入摘要。
- 最低 macOS、架构、Python 版本、Worker 协议版本。
- 锁文件摘要、实际 wheel 清单和 runtime 来源。
- 模型 ID、来源、revision、量化信息。
- 所有必需模型文件的相对路径、字节数和 SHA-256。
- 裁剪规则版本、签名方式和构建工具版本。

构建时执行完整校验；首次运行按发行版本检查资源，耗时较长的摘要读取放在后台并显示状态。后续正常启动可检查必要文件与已验证版本，避免每次在主线程重新读取约 700 MB。明确报告“资源缺失/损坏”，提供重新安装提示和诊断校验入口。

原生二进制签名会改变文件内容。输入归档/wheel 的摘要与最终签名产物摘要应分开记录；最终 App 文件清单可作为包外构建报告，DMG 摘要在所有步骤完成后生成，避免清单包含自身产生循环。

## 7. 降低使用故障的针对性加固

以下风险来自现有代码审查。实施时先补能验证问题的测试，再做局部修改，不把整套语音功能重写列为打包前提。

### 7.1 Worker 启动、重试、取消和退出

| 风险 | 当前结构 | 计划要求 |
| --- | --- | --- |
| 两个 Worker 同时加载 | `stop()` 发 terminate 后直接丢弃引用，没有确认退出 | 异步等待旧进程有界退出，再启动新进程；必要的终止只针对本 App 持有的子进程 |
| 旧启动任务停止新进程 | 启动轮询结束后存在无进程身份判断的 stop 路径 | 启动/超时/退出处理同时核对进程身份和 generation |
| 连续重试造成重叠 | `launch` / `retry` 创建未统一管理的 Task | 持有启动任务，去重/取消旧任务；退出后不允许旧任务重新启动 Worker 或回写 Ready |
| 加载错误被泛化 | Python 启动错误使用 `request_id: null`，客户端普通响应分支忽略无 UUID 帧 | 增加独立启动错误处理，保留资源、导入、动态库、模型等错误类别 |
| 空闲 Worker 退出但 UI 仍 Ready | Worker 退出会清理内部 ready 状态，App 未必及时获知 | 增加状态通知或等效机制，及时显示不可用/恢复中 |
| 退出入口不统一 | 菜单 quit 有停止逻辑，系统终止入口未统一 | 菜单退出、⌘Q、系统退出共用清理；录音/识别/加载中退出不留下继续运行的 Worker |

启动超时、推理超时和最多重试次数集中配置。文件缺失、摘要错误、系统不兼容等确定性错误不应重复等待三次长超时；瞬时进程错误采用有界重试。保持 UI 响应，不在主线程同步等待模型或进程退出。

测试覆盖慢 ready、无 ready、启动错误、旧 ready/result/EOF 晚到、连续重试、识别取消、加载时退出和 Worker 被终止。完成条件是无重复输出、无错误 Ready、无失控重启、无持续并存的模型进程。

### 7.2 首次授权与恢复

- 保留现有三项权限的检测和入口，提示每项权限用途与当前状态。
- 不把“打开系统设置”和“请求系统权限”混作同一行为；合并工作区已有相关改动时重新验证。
- 验证第一次允许、第一次拒绝、部分允许、在设置中补授予、撤销再恢复。
- 明确何时需要退出重开；重新打开或点击重试后，权限状态与 App 状态必须一致，成功后清除旧错误提示。
- 从 `/Applications/言落.app` 授权和验收；DMG 中直接运行时应引导先复制安装，避免权限绑定到临时位置。
- 保持现有 Bundle ID `com.local.VoxDrop`，减少数据和权限迁移变量。后续若更改，另行设计迁移，不在打包时顺手变更。

### 7.3 用户数据、临时音频和多实例

| 数据 | 目标位置/规则 |
| --- | --- |
| 历史、热词数据库 | 用户 `Library/Application Support/com.local.VoxDrop/`，DB/WAL/SHM 都在包外 |
| 偏好设置 | 保持稳定 Bundle ID 下的 UserDefaults |
| 依赖缓存 | 用户 `Library/Caches/com.local.VoxDrop/` 或明确的系统缓存位置 |
| 临时音频 | 本应用专用临时目录，并按实例/会话隔离 |
| 日志 | Unified Logging 或有轮转限制的包外日志；不记录正文、录音、热词全文 |
| runtime、模型 | App 内只读；运行时不修改、不补下载 |

当前残留清理会遍历共享会话目录中的 WAV，两个 App 副本可能互删文件。首版建议单实例运行，同时对临时目录按实例隔离；只清理已经确认过期且不属于活跃会话的本应用文件。

正常结束、取消、超时和退出都清理临时录音；强制退出无法保证即时清理，下次启动按规则回收。测试不能清空开发者真实历史、缓存或系统权限来模拟新用户，应使用专用测试账户/目录。

### 7.4 录音与输出回归

重点验证：无可用麦克风、音频引擎启动失败、临时目录不可写、磁盘空间不足、设备拔插、睡眠唤醒。当前音频启动在 `engine.start()` 前已创建文件和 tap，需要失败回滚；写文件失败不应静默输出成功。若复现，做局部错误传播和清理修复。

保留 Hold/Toggle/Esc、120 秒录音上限、热词预算、静音处理、剪贴板竞争检查和按键释放检查。不得自动发送聊天或邮件。

旧兼容性矩阵中的“识别期间切换窗口就跳过粘贴”与当前代码不一致：当前实现向发送时的系统焦点粘贴，没有那项焦点保护。打包验收按当前确认的行为对齐文档，不能把不存在的保护写成已实现，也不默认增加新的焦点策略。

闲置后首次识别变慢已有独立排查记录。发行前覆盖“不会永久卡住、可取消/恢复”的稳定性；预热、内存保留、大规模音频线程重构不默认随打包启用。

## 8. 签名与未公证分发

本版使用 ad-hoc 签名，不执行需要 Developer ID 的公证，不把 hardened runtime 或 notarization 当成已完成项目。

构建顺序：

1. 完成全部资源复制、可迁移路径调整、裁剪、版本写入和许可证收集。
2. 根据文件类型递归列出所有 Mach-O，包括无扩展名可执行文件、`.so`、`.dylib` 和内嵌组件。
3. 从内向外逐个签名原生代码及嵌套 Bundle，最后签整个 `言落.app`。`.metallib` 和模型文件作为资源封存，不盲目当 Mach-O 签名。
4. 每个原生组件分别执行严格签名验证；再执行整个 App 的 `codesign --verify --deep --strict`。
5. 完成真实启动和推理后，再次验证 App 签名和文件内容，确认没有运行时写入。
6. 制作 DMG；从 DMG 复制出来后再次验证签名及运行。

不能使用一次 `codesign --deep --sign` 替代逐层签名与资源枚举；尤其不能漏掉非标准目录里的 Python 扩展。签名后不再改 Info.plist、模型、Python 文件或依赖。

现有开发脚本使用仅 identifier 的自定义 designated requirement。其 TCC 表现不能推广为所有系统/更新都稳定，也不等同于可信开发者身份。发行签名规则需固定、评审并验证覆盖更新；不能为规避权限重授而放宽完整性检查。

验收区分：

- `codesign` 完整性通过：必须满足。
- Gatekeeper 无提示接受：无 Developer ID 的本版不承诺。
- 用户按说明允许打开后，App 与包内 Worker 能正常运行：必须满足。

`spctl` 的未公证拒绝不是签名内容损坏；工具执行内部错误也不能作为通过证据。必须通过浏览器下载带 quarantine 的真实文件验证用户流程。

安装说明采用 Apple 当前的单应用允许打开流程：“尝试打开 → 系统设置 → 隐私与安全性 → 仍要打开”。实际文字以目标系统实测为准。不能把“已损坏”一概当成普通未公证警告；先检查下载摘要和签名。不给用户要求关闭 SIP 或全局 Gatekeeper 的常规安装步骤。[Apple：安全打开 Mac 应用](https://support.apple.com/en-us/102445)

## 9. 压缩与 1 GB 体积控制

### 9.1 首选方式

DMG 内包含完整 `言落.app`、Applications 拖拽入口和简短安装说明。使用系统能直接挂载的压缩格式，用户不安装解压工具。

对同一份已签名 App，先比较 `UDZO` 与 `ULFO` 的真实输出大小、挂载时间和复制安装结果。`UDZO` 可尝试更高压缩等级；具体参数以构建机 `hdiutil` 帮助和实测为准。只在前两者不能达标时再评估 `ULMO` 等更强压缩，并重新验证最低系统的可读性与安装耗时。

最终发布一个验证通过的 DMG。不要在包内再放压缩模型并要求首次启动解压，也不要把两个压缩格式一起交给普通用户选择。

### 9.2 裁剪顺序

| 顺序 | 操作 | 验证要求 |
| --- | --- | --- |
| 1 | 排除仓库 `.git`、benchmark 数据、其他模型、其他后端环境、uv 下载缓存、旧构建 | 发行内容按白名单组装，不能复制整个项目 |
| 2 | 移除明确不用的测试数据、测试模块、构建头文件、开发工具与缓存 | 确认不被运行包导入；导入和真实识别回归 |
| 3 | 压缩格式/等级对比 | 挂载、复制、签名和启动全部通过 |
| 4 | 仅在仍超标时分析可选运行依赖 | 每组删除记录收益和依赖证据；恢复简单；完整功能回归 |

保留 `.dist-info` 和许可证；不要批量删除所有 JSON、模板、Metal 资源或认为无用的模型索引。不要直接删除 `huggingface_hub`、`transformers`、`scipy` 或所有非 Qwen 子模块，动态导入可能依赖它们。

每次裁剪只操作 staging 副本，保留未裁剪版本。先恢复有疑问的依赖，再调整压缩；包体积没有超过门槛时，不为少几 MB 冒额外兼容性风险。

### 9.3 自动体积检查

以最终 DMG 的逻辑字节数为准，不用 `du` 的磁盘占用或四舍五入后的 Finder 数字：

```sh
DMG_BYTES=$(stat -f %z "$DMG_PATH")
if [ "$DMG_BYTES" -ge 1000000000 ]; then
    echo "DMG 超过发布上限：$DMG_BYTES bytes" >&2
    exit 1
fi
```

950,000,000 字节以上给出内部预警。任何签名、附加内容或重新压缩后都重新测量。超标时构建判定失败，不能改成拆包、删模型或启动后下载来绕过要求。

## 10. 构建脚本与产物设计

### 10.1 已新增脚本

| 文件 | 职责 |
| --- | --- |
| `scripts/build-macos-release.sh` | 固定构建参数，构建 release Swift 主程序，协调组装 |
| `scripts/package-python-runtime.sh` | 从固定来源组装独立 Python、依赖与 Worker |
| `scripts/verify-macos-bundle.py` | 检查资源清单、架构、最低系统、动态链接、符号链接和源码路径依赖 |
| `scripts/sign-macos-app.sh` | 枚举并逐层 ad-hoc 签名及验证 |
| `scripts/smoke-packaged-worker.py` | 使用包内解释器执行真实协议、音频解码、模型/热词推理测试 |
| `scripts/create-macos-dmg.sh` | 组装卷、压缩、挂载校验、体积检查、生成摘要 |
| `scripts/verify-release.sh` | 汇总自动检查结果和人工验收证据，形成发布报告 |

脚本可按实现复杂度合并，但职责和失败判定应保持清楚。构建和验证工具只在维护者机器运行，不作为用户依赖。

### 10.2 构建可靠性

- 每次使用新的 staging 目录，目录中带版本/构建标识，防止旧资源混入。
- 不覆盖正在运行的开发 App，不删除用户模型或现有环境。
- 仅清理本次已知 staging 路径，挂载失败/中断也要回收本次卷；避免宽泛递归删除。
- Shell 启用错误传播和 `pipefail`，复制/压缩/签名失败均中止；不能只看到最后的字节计数就认为成功。
- 所有路径通过正确引用的参数传递，不依赖 shell 展开处理中文或空格。
- 编译模块缓存使用项目可写目录；记录 Swift、SDK、系统、uv、Python、依赖锁和实际 wheel。
- 构建输入冻结后生成候选；工作区有改动时记录其摘要，不能只标一个不包含改动的 commit。
- 生成用于调试的符号/报告并单独保存，不把它们作为用户必须下载内容。
- 可复现首先指输入、过程和结果可追溯；未测量前不承诺两次 DMG 字节完全相同。

### 10.3 最终交付物

```text
dist/
├── VoxDrop-0.1.0-macos-arm64.dmg
├── SHA256SUMS.txt
├── release-notes.md
└── release-validation.json
```

另在内部保存已签名 App、实际依赖清单、构建日志、裁剪记录和测试记录。Release 下载主体为 DMG，SHA-256 文件便于核对下载完整性。

许可证包括 Python、MLX、mlx-audio、各随包依赖及 Qwen/量化模型对应许可和必要声明。模型卡标明 Apache-2.0，但仍要取得并随包保留对应许可证文本及来源，不以一个模型卡标签代替全部分发材料。

## 11. 分阶段实施计划

| 阶段 | 工作 | 必须交付的结果 | 进入下一阶段条件 |
| --- | --- | --- | --- |
| P0 基线冻结 | 记录源码与工作区、锁、模型摘要；统一 macOS 26.2 目标 | 基线清单、更新后的系统要求、回归样本集合 | 输入清楚且现有功能基线可运行 |
| P1 runtime 验证 | 独立 Python、锁定依赖、Worker 安装、迁移目录、布局和签名试验 | 可迁移的 staging runtime 与真实推理结果 | 无外部 Python/模型路径依赖，布局验证通过 |
| P2 App 资源改造 | Bundle 配置、统一录音/清理目录、发行设置页、环境隔离 | 可脱离仓库启动的 release App | 从 Finder 运行，包内模型识别成功 |
| P3 稳定性加固 | Worker 生命周期、启动错误、退出、实例隔离及复现的音频失败修复 | 针对性测试和明确恢复行为 | 无已知发布阻断问题 |
| P4 发行组装 | 许可证、完整性清单、最终嵌套签名、只读运行 | 不再修改的候选 App | 运行前后完整性检查通过 |
| P5 压缩 DMG | UDZO/ULFO 比较、必要裁剪、挂载复制验收、体积检查 | 小于 1 GB 的候选 DMG 与 SHA-256 | 实际字节数达标，复制产物可运行 |
| P6 跨机验收 | macOS 26.2、独立 M 芯片、空缓存断网、权限、升级、长稳 | 逐项测试记录及已知限制 | 所有必过项有证据；缺设备的项目保持未验证 |
| P7 发布 | 准备 Release、上传相同候选、校验资产、正式公开 | 可下载的 DMG、摘要、发布说明 | 上线文件与验收文件一致 |

完成一阶段再推进其依赖阶段。许可证收集、安装文案、测试用例准备可穿插进行。若更改模型、runtime 或关键依赖，需要从受影响阶段重新验证，不沿用旧候选的通过结果。

## 12. 验收设计：避免“只在开发机能运行”

### 12.1 自动验证层次

1. 原有 Python 单元测试和 Swift 核心检查：保护既有协议、快捷键、输出和数据逻辑。
2. 发行路径测试：缺资源、中文/空格路径、App 搬迁、污染的环境变量和遗留 projectPath。
3. Worker 故障测试：使用可控 mock 子进程覆盖启动/退出/超时/重试竞态；不以真实模型拖慢每个单元测试。
4. 包内真实 Worker smoke：权重、tokenizer、音频读取和 Metal 推理全部实际执行。
5. App/DMG 检查：资源完整性、原生依赖、签名、运行后不改包、压缩字节数和挂载复制。
6. 人工系统集成测试：权限、真实麦克风、跨应用粘贴、Gatekeeper、安装升级和睡眠唤醒。

`ready` 只说明加载到某一步；必须拿真实 WAV 获得有效 `result`。Mock 通过不能代替真实 MLX，终端调用 Worker 通过不能代替 Finder 启动 App，开发机通过不能代替另一台 Mac。

### 12.2 干净环境要求

- 至少一台没有本项目源码、uv、Homebrew、外部 Python 环境和模型缓存的独立 M 系列 Mac。
- 在测试账户第一次安装 App；不能预先通过开发脚本写 projectPath。
- 使用完全空的受控应用缓存目录，断开网络后首次启动并识别。
- 仅有 macOS 自带组件；不运行项目环境搭建指引，不安装命令行工具或解压程序。
- 从真实下载的 DMG 拖入 Applications，推出 DMG，再启动 App；由此证明没有继续依赖挂载卷。
- 检查包内 Python 实际路径、加载库、模型位置及实际输出，不只看菜单栏有图标。

开发机可做受控目录迁移、网络隔离与环境污染试验，但不通过重命名/删除用户源码、卸载 Homebrew、清空真实账号数据来模拟干净机器。无源码机器或全新测试账户提供更可靠的独立证据。虚拟机可能缺少所需 Metal 能力，不能用无 GPU 的 VM 代替真机推理验收。

### 12.3 设备与系统矩阵

| 设备/系统 | 要求 | 验收范围 |
| --- | --- | --- |
| M1 或 M2，8 GB，macOS 26.2 | 最低系统与低内存主要验收机，需实际可用组合 | 全新安装、断网首轮、热词、长录音、恢复、持续运行 |
| 另一代 M 系列，macOS 26.2 或更新受支持版本 | 独立跨芯片验收机 | 安装、真实录音、Metal 推理、输出、升级 |
| 当前开发机 | 辅助回归 | 开发功能不回退、资源重定位与自动测试；不能独自完成跨 M 芯片验收 |
| macOS 26.1 或更低 | 不支持，做负向检查或元数据验证 | 系统明确拒绝/提示版本要求，文案不得宣传可用 |
| Intel Mac | 不支持 | 分发页面清晰标明 Apple Silicon，产物不误标 universal |

若不能取得 macOS 26.2 的实际测试环境，最低系统验证仍是未完成，不能用“wheel 标签吻合”代替。新硬件可能自带更高系统，按实际可安装的组合测试并记录，不要求降级到其不支持的系统。

### 12.4 功能与故障验收矩阵

| 编号 | 场景 | 通过标准 |
| --- | --- | --- |
| I01 | 浏览器下载，带 quarantine，挂载并复制 | 安装说明可执行；无资源丢失或签名损坏 |
| I02 | 推出 DMG 后启动，断网且空缓存 | 包内 Worker 加载并完成第一条真实识别，无下载/安装提示 |
| I03 | App 移动到带中文和空格的用户目录 | 同一包可运行，路径不是固定 `/Applications`；权限变化按系统提示恢复 |
| I04 | Bundle 不可写 | 识别、历史、热词可用；运行前后包内文件与签名不变 |
| I05 | 普通用户首次运行 | 不需要 sudo 安装依赖；目录权限遵循系统规则 |
| P01 | 三项权限逐个允许或拒绝 | 当前状态准确、缺失项可理解、补授权后能恢复 |
| P02 | 授权后退出重开、撤销后恢复 | 不无限“启动中”，不残留错误 Ready 或旧错误文案 |
| F01 | Hold、Toggle、Esc 和短词 | 无按键泄漏、重复触发、取消后输出或短词被过滤 |
| F02 | 中文、英文、中英混合、专业热词 | 与冻结基线比较，保留热词预算与语言归一化 |
| F03 | 静音、空音频、120 秒录音 | 无崩溃、无死锁；错误与取消可恢复，录音上限不退步 |
| F04 | 浏览器、微信、Office、Mail 测试输入区 | 一次粘贴；不自动 Enter/发送；不抢焦点 |
| F05 | 识别时切换窗口/复制其他内容/按键未松开 | 按当前产品规则输出；剪贴板竞争、按键保护不退步 |
| R01 | 启动中多次重试、退出，旧消息晚到 | 不出现双 Worker、旧任务干扰新任务、退出后重启 |
| R02 | Worker 失败、超时、被终止 | UI 能识别，有限恢复，不永久卡住或无限循环 |
| R03 | 麦克风拔插/切换、录音启动失败 | 释放文件/tap；再次使用可恢复 |
| R04 | 磁盘空间不足/临时目录不可写 | 明确失败，不保存伪成功，不污染后续会话 |
| R05 | 休眠唤醒、闲置后第一条 | 可完成或有界失败并恢复，取消有效 |
| D01 | 历史、热词、设置跨启动 | 内容保留，数据位于包外 |
| D02 | 退出旧版后覆盖安装 | 历史/热词/设置不丢；权限保留或重授流程已实测 |
| D03 | 同时打开两个副本 | 单实例规则有效；不互删 WAV、不重复粘贴 |
| D04 | 正常退出/强制退出后重开 | 正常退出无残留 Worker；过期音频安全回收 |
| S01 | 连续使用与低内存 | 无持续增长的进程/文件句柄，无可复现泄漏或崩溃 |
| Z01 | 最终压缩文件 | `< 1,000,000,000` 字节，摘要、挂载、复制、签名全部通过 |

建议首次公开前在 8 GB M 芯片机器上至少完成 100 次短输入、10 次取消/重试、多个长输入与一个 4 小时日常负载会话，记录内存趋势、进程数和失败次数。该数量是验收抽样，不代表数学上保证无 bug。

识别回归复用现有评测音频及评分脚本，在同机同条件下比较开发基线与发行 runtime。相同输入出现文本/热词能力变化时必须解释并确认；跨设备浮点差异不宜仅以逐字完全相等作唯一判断。记录冷启动和热推理耗时，明显变慢必须定位，不能仅因“最终能出结果”而忽略。

## 13. 发布、升级与回退

### 13.1 Release 发布步骤

1. 固定版本号和源码状态，版本在 Info.plist、文件名、资源清单和说明中一致。
2. 从本地已验收 DMG 生成 SHA256SUMS，不重新构建一份未经测试的上传包。
3. 准备 GitHub Release 草稿，附 DMG、摘要、安装说明和已验证平台记录。
4. 上传后检查资源字节数；下载抽检并比对 SHA-256。
5. 公布最低 macOS 26.2、Apple Silicon、模型已内置、无需额外依赖、未公证警告与权限步骤。
6. 公布实际验证的设备、已知问题和回退方法；所有发布必过项完成后公开。

正式上传和公开属于后续执行阶段；本文编写本身不代表已经上传或创建 Release。GitHub Release 单文件限制按其官方当前说明复核，安装包自身仍执行更严格的 1 GB 限制。

国内下载 GitHub 的速度是分发问题。可以以后增加同一 DMG 的国内镜像，但首发不依赖镜像服务，任何镜像文件必须保持相同 SHA-256；不得把模型改成网络下载来简化分发。

### 13.2 覆盖安装与回退

- 安装新版本前先退出旧 App 和 Worker，再由 Finder 替换 `.app`。
- 保持 Bundle ID、数据目录与首版数据库结构稳定，减少升级迁移变量。
- 不把运行中的 SQLite DB/WAL/SHM 当普通文件随意复制。若未来确需数据库迁移，使用一致快照/备份、版本迁移和失败恢复方案。
- ad-hoc 更新后的权限是否保留以实测为准，不能仅凭 Bundle ID 一致保证。
- 保留上一版 DMG 和校验值；回退先退出应用，再替换 App。无数据结构变更时验证旧包可读取现有数据。
- 卸载 App 与删除用户历史是两件事：移除 App 不自动删除历史、热词和设置，避免意外丢失。

## 14. 发布阻断条件与最终检查表

以下任一情况出现，停止公开发布并修复：

- 最终 DMG 达到或超过 1,000,000,000 字节。
- 用户需要安装任何外部运行依赖或首次联网下载模型。
- 从源码目录移出、推出 DMG 或断网后不能完成识别。
- 原生组件缺 arm64、依赖未打包动态库或最低系统高于 26.2。
- 只读运行失败、运行后签名变化、模型/资源校验错误。
- 已复现的重复 Worker、取消后输出、重复粘贴、历史丢失、永久卡住或不能恢复。
- 未完成独立 M 芯片 Mac 或最低系统关键验收。
- 真实下载后的允许打开/权限流程无法完成。

公开前逐项确认：

- [ ] macOS 26.2+ 与 arm64 声明、二进制和文档一致。
- [ ] runtime、Worker、模型全部内置，无开发机运行路径依赖。
- [ ] 包内解释器、原生库与 Metal 资源完整，未依赖 Rosetta/ffmpeg/Homebrew。
- [ ] 新用户、空缓存、断网第一次真实识别通过。
- [ ] App 搬迁、推出 DMG、只读 Bundle 验收通过。
- [ ] 权限允许/拒绝/恢复及未公证应用安装流程通过。
- [ ] Worker 生命周期、取消、重试、退出故障测试通过。
- [ ] 历史、热词、设置保留，临时音频与多实例规则通过。
- [ ] 既有语音、快捷键、剪贴板与粘贴行为回归通过。
- [ ] 最低系统及两代 M 芯片测试有实际记录，包含低内存场景。
- [ ] 所有原生组件及外层 App 签名验证通过，运行后不改包。
- [ ] 许可证、依赖清单、模型来源、构建记录完整。
- [ ] DMG 挂载/复制/运行通过，字节数严格小于 1 GB。
- [ ] SHA-256 对应最终候选，上传文件与验收文件一致。
- [ ] 安装、升级、回退说明与已知问题已经核对。

没有设备、系统权限或外部测试证据的项保留“未验证”，不能标为通过。“最大程度避免 bug”的落实方式是明确依赖、减少同时变化、验证真实分发路径、阻止已知故障进入版本，并保留可追溯和可回退的产物。
